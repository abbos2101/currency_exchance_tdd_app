name: currency_exchance_tdd_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc_concurrency: ^0.2.0
  cupertino_icons: ^1.0.2
  dio: ^4.0.6
  easy_localization: ^3.0.0
  flutter_bloc: ^8.0.1
  get_it: ^7.2.0
  provider: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true